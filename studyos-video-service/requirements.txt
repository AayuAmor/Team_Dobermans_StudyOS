# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Settings & validation
pydantic>=2.7.0
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# Async file I/O
aiofiles>=23.2.1

# Video download
yt-dlp>=2024.4.9

# Transcription (installs torch as a dependency)
openai-whisper>=20231117

# NOTE: ffmpeg must be installed at the OS level.
# Ubuntu/Debian: sudo apt-get install ffmpeg
# macOS:         brew install ffmpeg
# Docker:        see Dockerfile
