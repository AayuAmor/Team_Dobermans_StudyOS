# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Settings & validation
pydantic>=2.7.0
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# Async file I/O
aiofiles>=23.2.1

# Video download
yt-dlp>=2024.4.9

# Transcription (installs torch as a dependency)
openai-whisper>=20231117

# ffmpeg binary — bundled via imageio so no OS-level install is required.
# Falls back to the system ffmpeg if one is found on PATH.
imageio[ffmpeg]>=2.34.0

# Development / testing (not needed in production container)
# pytest>=8.0.0
# httpx>=0.27.0
